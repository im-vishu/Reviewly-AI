# Reviewly AI Frontend

React, TypeScript, Vite, Tailwind CSS, Supabase client, and the browser fallback analyzer live here.

Run from the repository root:

```bash
npm run dev
npm run typecheck
npm run lint
npm run build
```

The frontend reads environment variables from `frontend/.env`.
