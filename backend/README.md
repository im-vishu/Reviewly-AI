# Reviewly AI Backend

The local Node API and Supabase migrations live here.

Run from the repository root:

```bash
npm run backend
```

API endpoints:

```text
GET  /api/health
POST /api/reviews/analyze
```

Supabase migrations are in `backend/supabase/migrations`.
